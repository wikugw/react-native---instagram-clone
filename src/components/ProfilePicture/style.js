import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 43,
    margin: 5,
    borderColor: '#db0daf',
    borderWidth: 3
  },
  image: {
    borderRadius: 40,
    borderWidth: 1,
    borderColor: 'white'
  }
});

export default styles;